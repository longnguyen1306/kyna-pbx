import { Flex } from "@mantine/core";

const HomePage = () => {
    return (
        <Flex direction='column' gap={16} px={20}>
            homepage
        </Flex>
    );
};

export default HomePage;
