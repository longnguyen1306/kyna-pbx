import { Flex } from "@mantine/core";
import AccountInfo from "./AccountInfo";

const RightHeader = () => {
    return (
        <Flex align={"center"}>
            <AccountInfo />
        </Flex>
    );
};

export default RightHeader;
